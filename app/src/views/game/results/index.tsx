import React, { FC } from 'react'

import { ViewContainer, PageTitle } from '@/components/shared'

const Results: FC = () => {
  return (
    <ViewContainer>
      <PageTitle title='Results' shouldShowIcons={false} />
    </ViewContainer>
  )
}

export default Results
